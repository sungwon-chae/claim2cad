# Claim2CAD runtime + test dependencies.
# Pin majors only; exact versions are resolved by pip.

build123d>=0.7,<1.0
numpy>=1.26,<3.0
pydantic>=2.6,<3.0
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0
pytest>=8.0,<9.0
