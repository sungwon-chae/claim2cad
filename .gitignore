# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/

# Env / secrets
.env
.env.local

# Generated artifacts that we WANT to commit (golden, hinge, planetary_gear)
# are kept; transient logs and snapshots are ignored.
logs/*.log
docs/diagrams/*.tmp.*

# Node / viewer
viewer/node_modules/
viewer/dist/
viewer/.vite/

# viewer/public/data is generated by `make viewer-build`. Keeping it out
# of git keeps the repo small; CI regenerates before deploy.
viewer/public/data/

# OS
.DS_Store
Thumbs.db

# Patent fetch cache (regenerable from claim2cad.patent_collector)
.cache/
